import { NavMenuMobile } from './NavMenuMobile';
import { NavMenuDesktop } from './NavMenuDesktop';

export const NavBar = () => {
	return (
		<>
			<NavMenuMobile />

			<NavMenuDesktop />
		</>
	);
};
